Given a set of locations and the available one-way connections between them (e.g., flights or roads), determine whether there exists an itinerary that starts somewhere and visits every location exactly once, following only the allowed directions.