You are given an undirected weighted graph described by an edge list, where each edge is a triplet (u, v, w) denoting a two-way connection between u and v with weight w. Given two nodes, source and target, compute a shortest path between them. A shortest path is the one with the minimum possible total weight (sum of edge weights) over all paths connecting the nodes. Return only the path length as a floating-point value.