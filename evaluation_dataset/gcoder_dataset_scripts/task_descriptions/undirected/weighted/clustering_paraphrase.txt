You are given an undirected weighted graph as an edge list, where each edge is a tuple (u, v, w) denoting a bidirectional edge between u and v with weight w. For a specified node identifier node, compute the node’s local clustering coefficient, capturing how well its neighbors are interconnected while accounting for edge weights. Return only the coefficient as a floating-point value.