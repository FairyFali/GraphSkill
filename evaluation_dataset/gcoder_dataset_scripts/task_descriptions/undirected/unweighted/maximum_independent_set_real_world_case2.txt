You’re activating a network of wireless sensors. Let V be the sensors and E be a list of unordered pairs where each pair marks two sensors that mutually interfere if switched on together. These interference relations are symmetric and have no severity levels. Find the largest set of sensors that can be turned on simultaneously with no interfering pair. Return only the size as  int.