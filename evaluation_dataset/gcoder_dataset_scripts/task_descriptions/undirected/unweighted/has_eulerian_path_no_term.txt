You are given an undirected, unweighted graph represented by an edge list, where each edge is a pair of nodes (u, v) indicating a bidirectional connection. Decide whether the graph contains an Eulerian path. Such a path exists if and only if the vertices with nonzero degree form a single connected component and exactly zero or two vertices have odd degree. Return a boolean value.