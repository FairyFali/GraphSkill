You’re organizing a conference and need to schedule as many talks as possible in the same time slot. You have a list of talks V and a list of unordered pairs E where each pair indicates two talks that share a speaker or room and therefore cannot run at the same time. These conflicts are mutual and have no priority levels. Find the largest set of talks that can run simultaneously with no conflicts between any pair. Return only the size as  int.