You are given a directed, unweighted graph represented by an edge list, where each edge is an ordered pair (u, v) indicating a one-way connection from u to v. Compute the number of maximal groups of nodes in which every pair of nodes can reach each other via directed paths. Return only the count as an integer.