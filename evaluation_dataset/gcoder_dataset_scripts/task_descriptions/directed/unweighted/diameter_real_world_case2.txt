Imagine an airline network where each available direct flight is one-way, and you can only travel along the direction flights operate. Treat every flight as a single hop (all flights count equally). Compute the network’s diameter: the largest, over all pairs of airports, of the shortest number of one-way flights needed to travel from one airport to the other. Return the diameter as  int type.