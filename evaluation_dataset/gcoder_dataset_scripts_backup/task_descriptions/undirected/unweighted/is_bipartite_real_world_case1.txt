Given a undirected interaction network (unweighted), determine whether the entities can be split into two disjoint roles (e.g., buyers vs. sellers, producers vs. consumers) such that every connection goes from one role to the other and no connections exist within the same role. In other words, check if the network can be modeled as a two-sided system with all edges crossing between the two groups.