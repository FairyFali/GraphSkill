Given a undirected task-dependency map for a project (unweighted), determine whether the tasks can be divided into two phases (e.g., planning vs. execution) such that every dependency runs from one phase to the other and no dependencies exist within the same phase. In other words, check if the workflow can be organized as a two-stage process with all links crossing between the two stages.