Given an unweighted directed graph G = (V, E), determine whether it contains an Euler path—a trail that uses every edge exactly once. Such a path exists if and only if the graph is connected and exactly 0 or 2 vertices have odd degree.